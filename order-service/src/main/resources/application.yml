server:
  port: 8081

payment-service:
  base-url: http://localhost:8082

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/orders
    username: orders
    password: orders
  data:
    redis:
      host: localhost
      port: 6379
  flyway:
    enabled: true
